{
  "name": "money-bot",
  "version": "0.1.0",
  "private": true,
  "description": "Money Bot — Cursor marketplace plugin for human-approved UK/EU agent checkouts",
  "type": "module",
  "scripts": {
    "deploy": "wrangler deploy",
    "dev": "wrangler dev",
    "start": "wrangler dev",
    "cf-typegen": "wrangler types",
    "type-check": "tsc --noEmit",
    "test": "node --experimental-strip-types --test src/*.test.ts",
    "smoke:approve": "bash scripts/smoke-approve.sh"
  },
  "keywords": [
    "money-bot",
    "mcp",
    "cursor-plugin",
    "payments",
    "checkout",
    "uk",
    "eu"
  ],
  "license": "MIT",
  "dependencies": {
    "@modelcontextprotocol/sdk": "1.30.0",
    "agents": "^0.20.1",
    "zod": "^4.4.3"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^5.20260907.1",
    "typescript": "^5.8.3",
    "wrangler": "^4.105.0"
  }
}
